export type ArticleInfo = {
  id: string;
  title: string;
  img: string;
  tag: string[];
  view: number;
  post: string;
};

export type ArticleContent = {
  id: string;
  title: string;
  img: string;
  tag: string[];
  view: number;
  post: string;
  updated: string;
  content: string;
};
