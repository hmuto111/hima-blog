export type ArticleInfo = {
  id: string;
  title: string;
  img: string;
  tag: string[];
  view: number;
  post: Date;
};

export type ArticleContent = {
  id: string;
  title: string;
  img: string;
  tag: string[];
  view: number;
  post: string;
  description: string;
  content: string;
};
